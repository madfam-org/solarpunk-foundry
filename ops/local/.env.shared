# ============================================
# REVENUE SHIELD - Environment Configuration
# ============================================
# Mock environment for local Docker development
# Production: Replace with Kubernetes Secrets

# ============================================
# INFRASTRUCTURE - Database & Cache
# ============================================
DATABASE_URL=postgresql://madfam:secret@postgres:5432/madfam_db
REDIS_URL=redis://redis:6379/0

# ============================================
# JANUA - Identity Platform
# ============================================
# API Configuration
JWT_SECRET=dev-secret-change-in-production-min-32-chars
JWT_ISSUER=http://janua-api:8000
JWT_AUDIENCE=http://janua-api:8000
CORS_ORIGINS=http://localhost:3001,http://janua-dashboard:3000,http://digifab-quoting:3000
EMAIL_PROVIDER=console
EMAIL_FROM=noreply@local.dev
COOKIE_DOMAIN=localhost
LOG_LEVEL=info
API_WORKERS=2

# Dashboard Configuration
NEXT_PUBLIC_API_URL=http://localhost:8000
NEXT_PUBLIC_APP_URL=http://localhost:3001
SESSION_SECRET=dev-session-secret-min-32-chars-change-in-prod

# ============================================
# DIGIFAB-QUOTING - Cotiza Studio (Revenue Critical)
# ============================================
NEXTAUTH_URL=http://localhost:3000
NEXTAUTH_SECRET=dev-nextauth-secret-change-in-production-min-32
AUTH_PROVIDER_URL=http://janua-api:8000
INTERNAL_API_URL=http://janua-api:8000

# Payment Processing (Mock Keys)
STRIPE_SECRET_KEY=sk_test_mock_key_replace_with_real
STRIPE_PUBLISHABLE_KEY=pk_test_mock_key_replace_with_real

# Storage (Mock S3 - Use MinIO in production)
AWS_S3_BUCKET=cotiza-uploads-local
AWS_REGION=us-east-1
AWS_ACCESS_KEY_ID=minioadmin
AWS_SECRET_ACCESS_KEY=minioadmin

# Feature Flags
ENABLE_SUSTAINABILITY_SCORING=true
ENABLE_MULTI_CURRENCY=true
DEFAULT_CURRENCY=MXN
ENABLE_MESH_REPAIR=false
ENABLE_SUPPORT_DETECTION=true

# Analysis Settings
ANALYSIS_TIMEOUT_SECONDS=300
CORS_MAX_AGE_SECONDS=86400

# ============================================
# FORGESIGHT - Forge Sight Data Scraper
# ============================================
# Storage (MinIO Mock)
MINIO_ENDPOINT=http://localhost:9000
MINIO_ACCESS_KEY=minioadmin
MINIO_SECRET_KEY=minioadmin
MINIO_BUCKET=forgesight-data

# Search (OpenSearch Mock - Not in compose, optional)
OPENSEARCH_HOST=http://localhost:9200
OPENSEARCH_USERNAME=admin
OPENSEARCH_PASSWORD=admin

# API Configuration
JWT_SECRET=dev-forgesight-secret-min-32-chars-change-prod
CORS_ORIGINS=http://localhost:3002,http://localhost:3000

# Scraper Settings
CRAWLER_CONCURRENCY=3
CRAWLER_RATE_LIMIT_MS=2000
CRAWLER_USER_AGENT=ForgesightBot/1.0 (+https://forgesight.quest)

# Payment (Mock - Conekta for MX market)
CONEKTA_PRIVATE_KEY=key_mock_replace_with_real
CONEKTA_PUBLIC_KEY=key_mock_replace_with_real
CONEKTA_WEBHOOK_SECRET=webhook_secret_mock
CONEKTA_ENABLED=false

# Exchange Rates
EXCHANGE_RATE_API_KEY=mock_api_key
BANXICO_API_URL=https://www.banxico.org.mx/SieAPIRest/service/v1
BANXICO_TOKEN=mock_token

# ============================================
# SHARED - Application Settings
# ============================================
NODE_ENV=development
NEXT_TELEMETRY_DISABLED=1
PYTHONUNBUFFERED=1
TZ=America/Mexico_City

# Monitoring (Optional - Add if needed)
SENTRY_DSN=
POSTHOG_KEY=
PLAUSIBLE_DOMAIN=

# ============================================
# SECURITY NOTICE
# ============================================
# ⚠️  WARNING: These are MOCK credentials for local development only
# ⚠️  NEVER commit real secrets to git
# ⚠️  Production: Use Kubernetes Secrets or HashiCorp Vault
# ⚠️  Rotate all secrets before production deployment
