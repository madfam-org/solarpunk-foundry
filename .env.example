# =============================================================================
# SOLARPUNK-FOUNDRY - Shared Infrastructure Environment
# =============================================================================
# Copy this file to ops/local/.env.shared for local development
# DO NOT commit actual .env files to version control
# =============================================================================

# -----------------------------------------------------------------------------
# POSTGRESQL CONFIGURATION
# -----------------------------------------------------------------------------
POSTGRES_USER=madfam
POSTGRES_PASSWORD=madfam_dev_password
# Note: POSTGRES_DB is set in docker-compose.shared.yml

# -----------------------------------------------------------------------------
# REDIS CONFIGURATION
# -----------------------------------------------------------------------------
REDIS_PASSWORD=redis_dev_password

# -----------------------------------------------------------------------------
# MINIO CONFIGURATION
# -----------------------------------------------------------------------------
MINIO_ROOT_USER=minioadmin
MINIO_ROOT_PASSWORD=minioadmin

# -----------------------------------------------------------------------------
# REDIS DATABASE ALLOCATION
# -----------------------------------------------------------------------------
# DB 0: Janua (Authentication)
# DB 1: Cotiza (Quoting Platform)
# DB 2: Forgesight (Vendor Intelligence)
# DB 3: Dhanam (Financial Platform)
# DB 4: Fortuna API/Jobs
# DB 5: Fortuna NLP
# DB 6: BrepFlow (Collaboration)
# DB 7-15: Reserved for future services

# -----------------------------------------------------------------------------
# MINIO BUCKETS (Auto-provisioned)
# -----------------------------------------------------------------------------
# janua-uploads: User profile images, documents
# cotiza-stl-files: 3D model uploads for quoting
# forgesight-vendor-data: Scraped vendor information
# blueprint-harvester-models: 3D model repository

# -----------------------------------------------------------------------------
# NETWORK CONFIGURATION
# -----------------------------------------------------------------------------
# All services use: madfam-shared-network
# This network is created by docker-compose.shared.yml
# Other services should use: external: true
